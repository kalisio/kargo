version: '3.5'

services: 
  sections:
    image: ${VIGICRUES_SECTIONS_IMAGE}:${VIGICRUES_SECTIONS_TAG}
    environment:
      - DB_URL=${VIGICRUES_DB_URL}     
    deploy:
      replicas: 1
      placement:
        constraints:
         - node.role == worker
         - node.labels.vigicrues == true         
      restart_policy:
        condition: on-failure
    networks:
      - kargo-network

networks:
  kargo-network:
    name: ${DOCKER_NETWORK}
    external: true
          
