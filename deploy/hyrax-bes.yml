version: '3.5'

services: 
  bes:
    image: ${HYRAX_IMAGE}:bes-${HYRAX_TAG}   
    volumes:
      - ${HYRAX_BES_DATA_PATH}:/usr/share/hyrax
    deploy:
      replicas: 1
      placement:
        constraints:
         - node.role == worker
         - node.labels.bes = true
      restart_policy:
        condition: on-failure
    networks:
      - kargo-network

networks:
    kargo-network:
      name: ${DOCKER_NETWORK}
      external: true