version: '3.5'

services:
 geoserver:
    image: ${GEOSERVER_IMAGE:-localhost:5000/geoserver}:${GEOSERVER_TAG:-2.12.1}
    volumes:
      - ./../configs/geoserver:/opt/geoserver/data_dir    
      - ${GEOSERVER_DATA_PATH:-/mnt/data0/geoserver_data}:/mnt/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/geoserver"]
      interval: 1m
      timeout: 10s
      retries: 3
      start_period: 2m      
    deploy:
      labels:
        - "traefik.backend=geoserver"
        - "traefik.docker.network=${DOCKER_NETWORK:-kaabah}"
        - "traefik.enable=true"
        - "traefik.port=8080"
        - "traefik.frontend.entryPoints=http,https"
        - "traefik.frontend.rule=Host:geoserver.${SUBDOMAIN}"
      replicas: ${GEOSERVER_REPLICAS:-1}
      placement:
        constraints:
          - node.role == worker
          - node.labels.geoserver == true
      restart_policy:
        condition: on-failure
    networks:
      - kargo-network

networks:
    kargo-network:
      name: ${DOCKER_NETWORK:-kaabah}
      external: true
