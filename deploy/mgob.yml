version: '3.5'

services:
  mgob:
    image: ${MGOB_IMAGE}:${MGOB_TAG}
    configs:
      - source: mgob-rclone-config
        target: /rclone.conf
    volumes:
      - ./../configs/mgob:/config
      - ${MGOB_STORAGE_PATH}:/storage
      - mgob_data:/data
      - mgob_tmp:/tmp
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8090/status"]
      interval: 1m
      timeout: 10s
      retries: 3
      start_period: 1m
    deploy:
      replicas: 1
      placement:
        constraints:
         - node.role == manager
      restart_policy:
        condition: on-failure
    networks:
      - kargo-network        

configs:
  mgob-rclone-config:
    file: ${MGOB_RCLONE_PATH}

volumes:
  mgob_data: {}
  mgob_tmp: {}

networks:
  kargo-network:
    name: ${DOCKER_NETWORK}
    external: true

