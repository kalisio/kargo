apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: overpass-api-db
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.persistence.size }}
  storageClassName: {{ .Values.persistence.storageClass | quote }}