version: '3.5'

services:
 geoserver:
    volumes:
      - ./config/geoserver:/opt/geoserver/data_dir    
      - ${DATA_PATH}:/mnt/data
    networks:
      - kargo-network

networks:
    kargo-network:
      name: ${DOCKER_NETWORK}
      external: true