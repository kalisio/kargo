{
  "name": "kargo",
  "version": "0.18.0",
  "description": "A Docker based solution to build and operate Geospatial Platforms",
  "repository": {
    "url": "https://github.com/kalisio/kargo"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "release:major": "npm version --force major",
    "release:minor": "npm version --force minor",
    "release:patch": "npm version --force patch",
    "postversion": "git push origin HEAD --tags",
    "release:charts:major": "node scripts/release-charts major charts",
    "release:charts:minor": "node scripts/release-charts minor charts",
    "release:charts:patch": "node scripts/release-charts patch charts"
  },
  "devDependencies": {
    "glob": "^7.2.0",
    "js-yaml": "^4.1.0",
    "simple-git": "^3.1.1",
    "yesno": "^0.3.1"
  },
  "dependencies": {}
}
